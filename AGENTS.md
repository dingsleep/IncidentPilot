# IncidentPilot 协作约束

## 沟通与目标

- 默认使用中文沟通；代码、命令、类型名、环境变量和路径保持英文。
- 结论先行，发现方案存在工程风险时直接指出，不为了“多智能体”“自进化”等标签堆砌无效复杂度。
- 本项目的目标是可落地的 AIOps 事故诊断与人机协同处置平台，不是聊天机器人，也不是仅靠固定 Mock 返回值的演示。

## 唯一权威文档

- 开始任何设计或编码前，必须完整阅读 `E:\IncidentPilot\IMPLEMENTATION_MASTER.md`。
- `IMPLEMENTATION_MASTER.md` 是产品边界、架构、接口、目录、阶段顺序和验收标准的唯一权威来源。
- 如果实现需要偏离总文档，先在对话中解释原因、影响和备选方案，得到用户确认后再修改文档与代码；不得静默改变架构。
- 完成任务后，只能在验证证据充分时勾选对应复选框，并在文档末尾的“实施记录”追加日期、变更、命令和结果。

## 开始新会话时的固定动作

1. 完整阅读 `IMPLEMENTATION_MASTER.md`，尤其是“不可破坏的架构约束”“当前实施状态”和当前里程碑。
2. 先检查是否存在 `.git`；存在时运行 `git status --short --branch`，已有改动属于用户，保留并避免覆盖；不存在时按 M0.0 处理，不能假定仓库已经初始化。
3. 只处理当前里程碑中尚未完成的最小闭环，不跨阶段同时铺开多个半成品子系统。
4. 编码前先写失败测试；完成后运行当前测试、相关集成测试和阶段验收命令。
5. 汇报实际输出，不把未运行的测试描述为通过。

## 本地 Python 环境

- 用户允许使用现有解释器：`D:\software\ana\envs\tx_agent\python.exe`。
- 所有 Python 命令显式使用该解释器，不依赖终端当前激活状态：

```powershell
$PYTHON = 'D:\software\ana\envs\tx_agent\python.exe'
& $PYTHON -m pytest
```

- 当前已确认该环境为 Python 3.12.13。安装或升级依赖前，先展示依赖变更摘要。
- 项目依赖必须声明在 `pyproject.toml` 并生成锁定文件，不能只安装在环境中而不记录。

## 不可破坏的工程边界

- 多智能体采用“有界状态图 + 专职无状态子 Agent + 类型化共享状态”；禁止让多个 Agent 自由群聊或无限循环。
- Metrics、Logs、Traces 调查 Agent 只能获得各自需要的只读工具和最小上下文。
- 策略判定、权限校验、审批、执行、幂等、回滚和审计必须由确定性代码控制，不能交给 LLM 自行决定。
- 读工具和写工具位于不同 MCP 服务、使用不同凭据和权限范围。
- 不向模型提供任意 Shell、任意 SQL、原始 Docker Socket、任意 `kubectl exec` 或任意 URL 请求工具。
- 场景注入器和隐藏标准答案只对评测运行器可见，Agent 进程不得访问。
- 冻结 holdout 的私有执行规格只能由隔离的 Evaluation Runner 解密。日常实现 Agent、在线 API/Worker、候选生成器不得读取、解密或搜索 `E:\IncidentPilotPrivate` 与 `artifacts/private/`；只有用户明确发起“冻结评测/发布 holdout”任务时才能使用对应材料。
- 只记录模型的结构化结论、证据引用、工具调用和状态变化；不要索取或持久化私有思维链。
- 遥测数据必须来自实际运行的 OpenTelemetry Demo；测试替身只允许用于单元测试，不能冒充端到端结果。
- 所谓“自进化”只能生成候选版本，经离线回归、影子评测和人工批准后晋级；禁止生产 Agent 在线修改自身代码、提示词、权限或模型权重。

## 安全与 Git 红线

以下操作必须先获得用户明确授权：

- 删除文件、目录、数据库数据、容器卷或 Git 历史。
- 创建或修改 `.env`、密钥、Token、证书、生产凭据或 CI/CD 配置。
- `git commit`、`git push`、`git rebase`、`git reset --hard`、强制推送或创建 PR。
- 对外部署、公开发布、连接真实生产集群或执行真实生产写操作。

提交前先展示将提交的文件与变更摘要；commit message 使用简洁英文。

## 质量门槛

- Python：Ruff、Pyright、Pytest；前端：ESLint、TypeScript、Vitest；端到端：Playwright。
- 每个 MCP Tool 至少覆盖正常、参数错误、上游超时、权限不足和结果截断路径。
- 每个写操作必须覆盖未审批拒绝、重复调用幂等、执行失败、验证失败和回滚路径。
- 核心结果必须有确定性评测；LLM Judge 只能评价表达质量，不能替代根因、证据、权限与恢复判定。
- 阶段验收失败时先定位原因，不通过降低阈值、删除样本或放宽安全规则伪造通过。
- Train/validation 用于开发；冻结 holdout 只在候选和参数已经冻结后运行。看到 holdout 结果后不得继续针对同一 suite 调参；如需继续开发，必须版本化新的私有 holdout。
